"""Константы визуализации стратостата."""

import sys

import numpy as np


# ──────────────────── HUD ────────────────────
# На macOS используем системный Arial; на других платформах PyVista подберёт шрифт сам.
HUD_FONT_FILE: str | None = (
    "/System/Library/Fonts/Supplemental/Arial.ttf" if sys.platform == "darwin" else None
)

DEFAULT_AIR_PUMP_SPEED = 1000.0                # Скорость закачки баллона воздухом кг/с

# ──────────────────── Геометрия аэростата ────────────────────
BALLOON_RADIUS = 8.0                    # радиус оболочки (м)
ROPE_TOP_Z = -7.0                       # z крепления верёвки к оболочке (отн. центра)
ROPE_BOTTOM_Z = -15.0                   # z нижнего конца верёвки (отн. центра)
BASKET_OFFSET_Z = -2.0                  # смещение центра корзины от конца верёвки (м)
BASKET_SIZE = (5.0, 5.0, 4.0)           # (ширина, глубина, высота) корзины (м)
TARGET_RADIUS = 6.0                     # радиус визуального маркера цели (м)

# ──────────────────── Террейн ────────────────────
TERRAIN_RESOLUTION = 600                # число сегментов по каждой оси
TERRAIN_AMP_SIN = 5.0                   # амплитуда синусоидальных холмов (м)
TERRAIN_AMP_COS = 3.0                   # амплитуда косинусоидальных холмов (м)
TERRAIN_FREQ_SIN = 0.06                 # пространственная частота синусоиды (рад/м)
TERRAIN_FREQ_COS = 0.04                 # пространственная частота косинусоиды (рад/м)

# ──────────────────── Частицы ветра ────────────────────
NUM_PARTICLES = 200                     # количество частиц-трассеров
VISIBLE_RADIUS = 400                    # радиус видимости локальных частиц (м)
VISIBLE_Z_RANGE = 100                   # полуширина видимого диапазона высот (м)
STREAK_LENGTH = 2.0                     # длина штриха-хвоста частицы (м)
PARTICLE_SPEED = 2.0                    # множитель скорости движения частиц
WIND_SPEED_MAX_COLOR = 20.0             # скорость (м/с), при которой частица полностью красная

# ──────────────────── Анимация / тайминги ────────────────────
ANIM_INTERVAL_MS = 30                   # интервал таймера анимации (мс)
MIN_TICK_INTERVAL_S = 0.02              # мин. интервал между тиками (с), ~50 fps
MAX_FRAME_DELTA_S = 0.25                # макс. dt за один тик (защита от пауз/свёртки)

# ──────────────────── Энергия / телеметрия ────────────────────
# ──────────────────── Окно ────────────────────
WINDOW_SIZE = (2560, 1440)              # (ширина, высота) окна (пикс.)

# ──────────────────── Камера ────────────────────
# Начальное смещение камеры от аэростата (вектор «фокус → камера»)
CAMERA_INITIAL_OFFSET = np.array([150.0, -150.0, 80.0], dtype=np.float32)
CAMERA_INITIAL_VIEW_UP = (0.0, 0.0, 1.0)   # вектор «вверх»
# Камера всегда на фиксированном расстоянии от шара
CAMERA_ORBIT_RADIUS = float(np.linalg.norm(CAMERA_INITIAL_OFFSET))
CAMERA_DIRECTION_EPS = 1e-9             # порог вырождения направления камеры
